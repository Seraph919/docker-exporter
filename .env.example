# PostgreSQL connection settings (libpq standard variables)
PGHOST=postgres
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=transdb

# Exporter configuration
PORT=8090
LOG_LEVEL=INFO

# Optional: Nginx status endpoint for metrics (requires nginx container)
NGINX_STATUS_URL=http://nginx/nginx_status