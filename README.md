# Docker-exporter
A lightweight Prometheus-style metrics exporter for the Docker Engine. It interfaces directly with the Docker Unix socket to provide real-time observability into container states, resource consumption (CPU/Memory), and engine health. Designed for seamless integration into monitoring stacks using Prometheus and Grafana.
